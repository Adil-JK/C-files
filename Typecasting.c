main() {
	int a = 5;
	float b = 8.5;
	float c = (float)95/2; /* Typecasting of integer value into float. In order to get the result in
	float, one of the two values must be in float so we will get the result in float. In this case
	we have converted 95 to float so we will get the output i.e 47.5 in float. */
	
	printf("The value of a is %d \n", a);
	printf("The value of b is %f \n", b);
	printf("The value of c is %f", c);
	
} 
